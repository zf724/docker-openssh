version: "3.0"
services:
  openssh:
    build:
      context:
        https://raw.githubusercontent.com/zf724/openssh/master/Dockerfile  
    hostname:
      ct-ssh
    ports:
      - "2222:22"
    volumes:
      - /docker/keys:/root/.ssh/keys
    restart: always
